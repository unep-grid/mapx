{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "${version}",
    "pushRepo": "github",
    "requireUpstream": false
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "before:init": [
      "npm run test:app",
      "npm run doc:sdk"
    ],
    "before:bump": [
      "git fetch --tags -q && git tag -l \"${version}\" | grep -q \"^${version}$\" && { echo \"ERROR: tag ${version} already exists (possibly released on another branch). Update version.txt to the last released version first.\"; exit 1; } || true",
      "./release_scripts/update_versions.sh ${version}"
    ],
    "after:bump": [
      "./release_scripts/update_changelog.sh ${version}"
    ]
  }
}
